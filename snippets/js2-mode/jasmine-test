# -*- mode: snippet -*-
# name: js-test
# key: ;jst
# --
describe('$0', () => {
    beforeEach(module('app'));
    describe('', () => {
        beforeEach(() => {
        });

        it('', inject(() => {

        }));
    });
});
